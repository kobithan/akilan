import 'package:flutter/cupertino.dart';

class ReportInfoWidget extends StatelessWidget {
  const ReportInfoWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
